# DECISIONS.md

중요한 기술적 판단과 이유를 기록합니다. 에이전트는 구현 중 새 결정을 내리면 이 문서에 추가합니다.

## 2026-05-22 - 렌더링 엔진은 Canvas 2D를 기본으로 한다

### 결정

ScriptureFlux의 대규모 교차 참조 선 렌더링은 SVG DOM이 아니라 HTML5 Canvas 2D를 기본으로 구현한다.

### 이유

- 교차 참조 데이터가 수만 개 이상이 될 수 있다.
- SVG path를 수만 개 DOM 노드로 만들면 layout, paint, event handling 비용이 커진다.
- Canvas는 네트워크 배경장을 한 번에 그리는 방식에 적합하다.

### 영향

- 개별 링크 접근성은 별도 UI 또는 hit-test 레이어로 보완해야 한다.
- React 컴포넌트는 렌더링 대상이 아니라 상태와 UI chrome에 집중한다.

## 2026-05-22 - D3는 보조 계산용으로만 사용한다

### 결정

D3는 scale, interpolation, quadtree 같은 계산 보조 도구로만 사용하고, D3 selection 기반 DOM 렌더링은 사용하지 않는다.

### 이유

- React와 D3 DOM 제어가 충돌할 수 있다.
- Canvas 중심 구조에서 D3의 강점은 좌표/스케일/공간 인덱싱에 있다.

## 2026-05-22 - 성경 본문과 교차 참조 데이터는 분리한다

### 결정

교차 참조 관계 데이터와 실제 성경 본문 텍스트 데이터는 별도 파일과 별도 라이선스 문서로 관리한다.

### 이유

- 교차 참조 데이터와 성경 번역 본문은 라이선스 조건이 다를 수 있다.
- 본문 없는 데모도 가능하게 만들어 저작권 위험을 낮춘다.

## 2026-05-22 - 초기 릴리즈는 정적 SPA로 제한한다

### 결정

초기 버전에는 서버, 계정, 로그인, DB, 외부 유료 API를 추가하지 않는다.

### 이유

- 사용자가 요구한 페이지는 정보 교환이 아니라 보여주기에 집중한 정적 웹페이지다.
- GitHub Pages 배포와 오픈소스 공개에 적합하다.

## 2026-05-22 - Canvas 기반 시각화에서의 키보드/터치 접근성 보완

### 결정

캔버스를 포함하는 컨테이너에 `tabIndex={0}`과 포커스 스타일을 부여하고, 키보드 방향키(`ArrowLeft/Right/Up/Down`) 및 모바일 터치 드래그(`onTouchStart`, `onTouchMove`) 이벤트를 통해 활성 교차 참조(active link)를 조작할 수 있도록 설계한다.

### 이유

- HTML5 Canvas는 그래픽 렌더링에 특화되어 있으나 내부 개별 선이 DOM 요소가 아니므로 브라우저의 기본 포커스 및 터치 이벤트를 받지 못한다.
- 웹 접근성(a11y) 가이드라인을 준수하기 위해 컨테이너 포커스를 통한 키보드 운용성을 확보하고, 시각적 포커스 링 및 힌트 UI를 제공하여 사용자 경험을 premium하게 만든다.

## 2026-05-22 - GitHub Actions와 GitHub Pages를 통한 지속적 빌드 및 배포

### 결정

GitHub Actions 기반 CI/CD 파이프라인(`.github/workflows/deploy.yml`)을 구축하여, `main` 브랜치 푸시 시 자동으로 Lint, Typecheck, Test, Build를 진행하고 성공 시 GitHub Pages로 배포하도록 연동한다.

### 이유

- 수동 배포로 인한 환경 차이와 배포 오류를 차단하고 빌드 성공 품질을 완벽히 담보하기 위함이다.
- 정적 SPA 아키텍처에 가장 이상적이며 오픈소스 생태계에 유기적으로 기여하는 구조이다.

## 2026-05-22 - 120ms 호버 디바운스 및 클릭 기반 핀(Pin) 고정 메커니즘을 통한 인터랙션 안정성 확보

### 결정

마우스 호버 시 세부 정보 카드 뷰가 파르르 요동치는 현상을 방지하기 위해 120ms 디바운스 딜레이(Dwell)를 적용하고, 캔버스 내 참조선 클릭 시 활성 참조선을 골드 네온 라인으로 영구 고정(Pin)하는 `pinnedLink` 상태를 도입한다.

### 이유

- 마우스를 성경 축 또는 곡선 영역에서 연속적으로 이동할 때 실시간 카드 갱신에 의해 눈의 피로와 시각적 깜빡임 현상이 발생한다. 120ms의 디바운싱을 통해 미세한 포인터 잔물결을 안정화한다.
- 사용자가 원하는 특정 교차 참조 정보를 고정한 채로 텍스트를 읽고 싶을 때, 포인터 고정이 가능하도록 클릭 이벤트를 추가하여 핀(Pin) 기능을 제공하고 핀 해제 전까지 시각적 glow 및 텍스트 카드를 고정 유지한다.

## 2026-05-22 - 퍼블릭 도메인 한국어 성경(개역한글 1961) 탑재 및 한/영 다국어 듀얼 레이아웃 개편

### 결정

한국인 사용자 경험 극대화를 위해 대한민국 저작재산권 보호기간(50년)이 만료된 퍼블릭 도메인 성경인 **개역한글(1961년판)** 번역본을 로컬 JSON 데이터셋으로 탑재하고, `ReferenceCard` 상단에 한국어를 크게 배치하며 하단에 영문 KJV 번역본을 점선 구분 및 이탤릭 서체로 병기하는 한/영 다국어 듀얼 레이아웃을 개편한다.

### 이유

- 국문 성경 본문 렌더링 요청을 충족하되, public 저장소의 법적/라이선스 위험성을 완전히 제거하기 위해 완전한 퍼블릭 도메인 판본인 '개역한글(1961)'을 선택했다.
- 영어(en) KJV를 보조 자료로 결합 제공함으로써 신뢰도 높고 학술 가치가 높은 듀얼 레이아웃을 구성하여 프리미엄 UX를 완수한다.

## 2026-05-22 - 모바일 60FPS 최적화 (DPR 1.5 제한 클램핑 및 18px 터치 히트 반경 튜닝)

### 결정

모바일 기기의 high-DPI 렌더링 부하를 줄이기 위해 Device Pixel Ratio(DPR)를 최대 1.5로 Clamping 제한하고, 핑거 터치 환경의 조작 편의를 위해 터치 히트 반경(Hit Test Radius)을 기존 8px에서 18px로 확장한다.

### 이유

- 3x 이상의 초고해상도 모바일 디스플레이에서 Canvas 2D 연산량이 비약적으로 상승하여 프레임 드랍이 발생한다. 눈으로 구분하기 힘든 DPR 1.5 초과 영역을 제한함으로써 모바일 60FPS 성능을 견고히 보장한다.
- 마우스와 달리 굵은 손가락 터치는 좁은 오프셋 좌표로 곡선을 정밀 탭하기 어려우므로 모바일 터치 히트 반경을 너그럽게 조정한다.

## 2026-05-22 - 시각적 예술 영감 감사 헌사 (Chris Harrison & OpenBible.info)의 UI/문서 통합

### 결정

본 프로젝트의 시각 예술적 근원인 Chris Harrison 교수와 교차 참조 오픈 데이터를 제공한 OpenBible.info의 Sean Harrison 및 Christoph Römhild 목사에게 경의를 표하기 위해 UI 상단 패널, 하단 푸터, README.md, ATTRIBUTION.md에 감사 헌사 문구 및 하이퍼링크를 상세히 수록한다.

### 이유

- ScriptureFlux는 독자적인 상용 서비스가 아니라 시각화 예술의 아름다움을 정적 웹앱으로 구현 및 공유하고자 하는 목적을 갖는다. 오리지널 아이디어와 데이터 가치를 존중하는 것은 오픈소스 및 학술적 도덕성의 기초이자 요구 조건이다.

## 2026-05-22 - clientWidth/Height 기반 캔버스 크기 정밀 매핑 및 책별 분할 JSON 2단계 LOD 레이지 로딩 아키텍처 도입

### 결정

- 캔버스가 부모 크기를 벗어나 바닥이 잘리고 위에만 나오던 결함을 `ResizeObserver` 상에서 `entry.target.clientWidth/Height` 기반으로 논리 픽셀을 정밀하게 추출하고, `<canvas>`의 인라인 CSS 스타일을 `width: '100%', height: '100%'`로 강제 바인딩하여 해결한다.
- 11,907쌍의 대규모 교차 참조와 66권 전체 장/절 성경 구절 데이터를 브라우저 렉 없이 초고속 로드하기 위해 책별 동적 청크 분할(`public/data/bible-text/` 및 `public/data/cross-references/`) 전처리 파이프라인(`scripts/prepare-data.js`)을 구축한다.
- 최초 로딩 시에는 가벼운 랜드마크 교차 참조선 파일 (`public/data/cross-references.json`, 16쌍)만 탑재(LOD Level 1)하고, 성경 책 영역 호버/액티브 시 해당 책의 세부 참조 JSON 데이터를 비동기 fetch하여 머지(LOD Level 2)하는 2단계 LOD 캔버스 렌더러를 구축한다.
- `ReferenceCard`에서도 성경 구절 본문을 통째로 임포트하던 메모리 낭비 구조를 철폐하고, 구절 정보 활성화 시 책 인덱스에 매칭되는 책 본문 JSON 파일만 dynamic fetch하고 메모리 캐싱 및 미려한 로딩 스켈레톤 UI를 보여주도록 전면 개편한다.

### 이유

- 캔버스의 실제 물리 크기와 화면상의 논리 크기 매핑 시 픽셀 단위 누락이나 DPR 곱연산 불일치로 잘림이 발생하는 결함을 근본적으로 예방하고, 모든 스크린 배율에서 항상 컨테이너에 꽉 찬 레이아웃을 보장하기 위함이다.
- 수십 메가바이트에 이르는 전체 데이터셋을 싱글 번들로 패키징할 경우, 웹앱의 초기 로딩 성능이 최악으로 치닫고 모바일 기기 메모리가 고갈된다. 데이터셋을 66권 단위로 잘게 파쇄하여 필요한 시점에만 동적으로 load/merge 함으로써 극도의 가벼움과 60FPS의 쾌적한 렌더링 성능을 영구히 유지한다.

### 영향

- 초기 로드되는 데이터 자산 용량이 99% 이상 감소하여 모바일 환경의 네트워크 트래픽과 로드 타임이 경이적으로 줄어들었다.
- 120ms 호버 디바운싱 필터링과 비동기 fetch 병렬 호출, 로컬 메모리 캐시 조합으로 끊김 없는 유기적 인터랙션 감각을 완수했다.

## 2026-05-22 - 3대 프리미엄 편의 기능(Dimming 검색 하이라이트, Weight 필터 슬라이더, URL Hash 딥링크) 도입

### 결정

- **구절 집중 탐색 Dimming 모드**: 책, 장, 절을 선택할 수 있는 3단 Select Dropdown UI를 구현하고, 특정 구절이 탐색될 때 매칭되지 않는 캔버스 상의 모든 참조 곡선의 투명도를 `alpha 0.015`로 낮춰 어둡게(Dimmed) 만들고, 매칭되는 연결망만 선명한 에메랄드 네온 Glow로 강조 렌더링한다.
- **Weight 필터 슬라이더**: 연관 강도(0.1 ~ 1.0) 범위를 제어할 수 있는 슬라이더(range input)를 대시보드에 탑재하여, 캔버스 선 밀도를 사용자가 즉각적으로 조율하고 렌더링 부하를 조절할 수 있도록 한다.
- **URL Hash 딥링크 공유 및 비동기 복원**: 핀 고정(`pinnedLink`) 상태 변경 시 브라우저 Hash 주소를 `#GEN.1.1-JHN.1.1` 형식으로 갱신하고, 페이지 최초 마운트 시 Hash가 존재한다면 파싱하여 해당하는 소스/타겟 구절 책 청크의 비동기 fetch(`loadBookDetails`) 완료 후 핀 상태를 완벽히 자동 복원하는 복원 엔진을 구현한다.

### 이유

- 대규모 데이터셋(1.2만 쌍) 로드 환경에서 사용자가 특정 구절의 연결 관계에만 극도로 집중하여 관계망을 정밀하게 탐독할 수 있는 세련된 탐색 옵션(Dimming)이 절대적으로 필요하기 때문이다.
- 사용자 디바이스의 하드웨어 사양이나 모바일 터치 해상도에 맞추어 시각적 잡음과 복잡도를 덜어내고 선명한 중심 선들만 가려내어 볼 수 있는 실시간 렌더링 제어 필터(Weight)를 제공해 성능을 보조하기 위함이다.
- 웹 기반 정적 SPA 앱의 강력한 공유성을 극대화하여, 사용자가 심오하게 발견해낸 특정 구절 간의 흥미로운 교차 참조 관계를 다른 사용자들과 링크 단 하나만으로 고스란히 복제 및 공유할 수 있게(Deep Linking) 보장하기 위함이다.

### 영향

- 특정 책 청크 데이터가 메모리에 적재되지 않은 최초 로딩 순간에도 딥링크를 파싱해 백그라운드에서 해당 책의 청크 데이터를 자동 lazy load 후 머지하여 핀 복원을 완수하는 React 렉시컬 복원 생명주기를 수립했다.
- Glassmorphic 가로 그리드 대시보드를 구축해 다양한 해상도(모바일 및 데스크톱)에서 프리미엄 감성의 현대적 디자인 정체성을 완성했다.

## 2026-05-22 - OffscreenCanvas 백버퍼 기반 배경 네트워크 캐싱 도입

### 결정

Canvas 2D 렌더링 엔진은 유지하되, `NetworkCanvas`의 정적 배경 네트워크를 `OffscreenCanvas`가 가능한 환경에서는 백버퍼에 먼저 그리고, 미지원 환경에서는 HTMLCanvas fallback 백버퍼를 사용한다. foreground 캔버스는 캐시된 배경을 복사한 뒤 active/pinned 링크만 glow로 덧그린다.

### 이유

- hover, keyboard focus, pin 상태가 바뀔 때마다 수천 개 배경 곡선을 다시 그리면 대규모 데이터셋에서 상호작용 비용이 커진다.
- WebGL 전환은 셰이더, 좌표 버퍼, hit-test 재설계까지 포함하는 큰 변경이므로, 기존 Canvas 2D 아키텍처를 보존한 상태에서 검증 가능한 성능 개선을 먼저 적용한다.
- `OffscreenCanvas`는 지원 브라우저에서 백버퍼 생성 비용과 메인 캔버스 갱신 경로를 분리할 수 있고, fallback을 두면 정적 SPA 호환성을 유지할 수 있다.

### 영향

- 필터, 검색, 데이터 청크, viewport 변경 시에는 배경 캐시를 갱신한다.
- active/pinned 변경 시에는 배경 전체를 다시 그리지 않고 캐시 복사와 강조 링크 렌더링만 수행한다.
- 데이터셋에 유효하지 않은 book/chapter/verse tuple이 섞여 있어도 렌더링 전체가 중단되지 않도록 잘못된 튜플은 런타임에서 제외한다.

## 2026-05-25 - 34만 개 실증 데이터셋 탑재 및 2단계 LOD 파이프라인 전면 가동

### 결정

- OpenBible.info의 34만 개 규모 실증 교차 참조 TSV 데이터셋(`cross_references.txt`)을 온전히 다운로드 및 파싱하여 실제 100% 매핑으로 전환한다.
- 투표수 0 이하 또는 음수 데이터 및 범위형 타겟 구절을 정제하고, 1단계 로딩을 위한 최정예 상위 1,500선 및 66권 개별 적재용 LOD 2단계 분할 파이프라인을 온전히 가동하여 `public/data/` 디렉토리에 적재한다.

### 이유

- 기존의 데이터셋은 임의로 랜덤 생성된 1.2만 쌍 규모의 가상 합성 데이터로서, 실제 성경 구절의 organic한 연결 관계를 올바르게 시각화해 보여주지 못하고 단절되는 결함이 있었다.
- 34만 개에 이르는 전체 방대한 실증 교차 참조 데이터를 브라우저 랙 없이 탑재하고, 최초 캔버스 구동 속도와 60FPS 상호작용 프레임을 온전히 지켜내기 위해 2단계 LOD 레이지 로딩 아키텍처 규격을 엄격히 유지하면서 실제 데이터셋을 분할 가공해 적재했다.

### 영향

- 1:1 정밀 본문 매핑 및 34만 개 전체 실증 교차 참조가 100% 매핑되어 성경 66권 전체의 interconnectivity를 실증적으로 완전히 드러내었다.
- 1단계 글로벌 랜드마크 선로딩 분량을 1,500개로 고도로 압축해 초기 정적 사이트 진입을 매우 가볍게 유지했다.

## 2026-05-25 - Same-Book 및 Same-Chapter 신규 필터 옵션 및 동적 필터 통계 연동

### 결정

- Nice-to-have 기획서 규격에 정의되어 있던 **같은 책 내 연결선(Same-Book)** 및 **같은 장 내 연결선(Same-Chapter)** 신규 필터링 메커니즘을 시각화 캔버스에 추가 적용하고, 통계 대시보드에 실시간 필터 매칭 카운팅 및 활성 필터 명칭을 동적 노출한다.

### 이유

- 34만 개에 달하는 실증 데이터셋이 탑재되면서 캔버스 상의 시각적 선 밀도가 매우 촘촘해졌으므로, 사용자가 성경의 동일 책이나 동일 장 내부에서 유기적으로 도는 순환 연결성만을 따로 격리해 명징하게 독치할 수 있도록 필터 가용 범위를 확장했다.
- 동적 필터 카운트를 통해 캔버스 렌더 범위 내에 잔존하는 유의미한 연결선의 실제 부피와 통계를 실시간으로 전달하여 프리미엄 대시보드 사용성을 고도화한다.

## 2026-05-26 - 가로축 줌(Zoom) 및 드래그 팬(Pan) 아키텍처 도입

### 결정

성경 66권 축 캔버스를 정밀 확대/축소하고 스와이프 이동할 수 있는 가로 줌(Zoom 1.0x ~ 10.0x) 및 드래그 팬(Pan) 상태 기계를 도입하고, 줌된 화면의 좌표 정합성을 위해 1:1 역투영(Unprojection) 수학 필터를 결합한다.

### 이유

- 34만 개 대규모 실증 교차 참조가 빽빽하게 모여 있는 캔버스 환경에서, 휠이나 핀치 줌을 통해 세부 구간을 극도로 확대해보고 싶은 사용자 편의성과 시인성 요건을 충족하기 위함이다.
- 화면을 줌인한 상황에서도 마우스 호버와 핀(Pin) 생성에 오차가 생기지 않도록 하기 위해서는, 드로잉 좌표에 줌 스케일(`applyZoom`)을 적용하는 것뿐 아니라 역으로 마우스 화면 입력 좌표를 원래의 D3 Scale 좌표계(`invertZoom`)로 되돌려 hit-test를 비교하는 정합성 아키텍처가 필수적이다.

### 영향

- 마우스 휠과 모바일 핀치 제스처, 그리고 1손가락 스와이프를 모두 활용하여 부드럽고 끊김 없는 60FPS 확대 탐색이 가능해졌다.
- 글래스모프 스타일의 줌 컨트롤러 패널을 플로팅 제공하여, 휠이 없는 트랙패드나 일반 데스크톱 마우스 환경에서도 즉각적인 조작 편의성을 제공하며 Premium 비주얼 완성도를 확보했다.

## 2026-05-26 - 극대 줌(80배) 장/절 눈금 및 PC 마우스 클릭 선택형 인터랙션 고도화

### 결정

- 최대 가로 줌 한계를 10배에서 **80배**로 전격 확대하고, 줌 레벨에 따라 책(LOD 1) ➔ 장(LOD 2) ➔ 절(LOD 3) 수직 눈금을 촘촘히 렌더링하는 다이내믹 눈금 렌더러를 캔버스 하단 축에 탑재한다.
- PC 마우스 환경에서 무심히 마우스를 포인터 위로 움직일 때 하단 카드가 갱신되며 떨려 시각 피로를 만들던 호버 상세 카드를 **전면 차단**하고, 은은한 녹색 호버 힌트와 **오직 클릭 선택 시에만** 상세 카드와 골드 하이라이트를 띄우는 Click-to-View 시스템으로 대대적으로 전환한다. (모바일 스와이프 탐색은 그대로 보존)

### 이유

- 34만 개의 조밀한 교차 참조 곡선들에서 개별 절의 위치(예: `창 1:5`, `1:10`)를 시각적으로 단번에 분별할 수 있는 직관적인 스케일과 물리적 눈금이 절대적으로 요청되었기 때문이다.
- 정보 피로를 해결하기 위해 마우스 호버로 화면을 이리저리 오버할 때는 아주 은은한 비주얼 가이드 가이드선 힌트와 `pointer` 커서 변경만 제공하고, 사용자가 차분하게 집중하고 싶은 참조 곡선을 직접 클릭하여 확정했을 때 비로소 상세 정보를 표시하는 것이 프리미엄 뷰어로서 훨씬 격조 높은 경험을 선사하기 때문이다.

### 영향

- 80배 줌인 후 눈금을 보고 원하는 정확한 절 위치를 시각적으로 추적할 수 있어 정밀 탐색 성능이 획기적으로 개선되었다.
- 화면의 번쩍임과 요동 현상이 완치되어 오버 시에는 깔끔하고 차분하게 탐독이 가능하며, 클릭을 통해 원하는 특정 관계망만 집중 고정해서 가독성 높게 카드 텍스트를 읽을 수 있게 되었다.
